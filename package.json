{
  "name": "ui-qa-agent",
  "version": "1.0.0",
  "description": "GitHub Action for UI QA - compares PR screenshots against Figma designs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist/bundle",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "keywords": [
    "github-action",
    "ui-qa",
    "figma",
    "jira",
    "visual-testing"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.2"
  }
}

